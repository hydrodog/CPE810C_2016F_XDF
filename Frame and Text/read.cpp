#include "read.h"
#include <stdlib.h>
#include <map>
#include <string>
#include <fstream>
#include <iostream>
using namespace std;

void Read::readContent(string a) {
    string b = a;
    //if(b)break;
    //m.insert(pair<int,char>(i,b));
}
